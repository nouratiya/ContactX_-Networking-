# ContactX_-Networking-
This diagram shows the logical topology of the ConnectX network. A hybrid design is used, combining a ring between branch routers with direct links from HQ for improved performance and redundancy. Each branch follows a star LAN structure, while OSPF ensures fast routing and automatic rerouting in case of link failure.
